package app.test.Service;

import static org.assertj.core.api.Assertions.assertThat;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.context.SpringBootTest;

import app.service.AprimoramentoService;
/*
@SpringBootTest
@AutoConfigureMockMvc
public class TestAprimoramentoServiceIntegration {
	@Autowired
	private AprimoramentoService aprimoramentoService;
	
	@Test
	@DisplayName("Cena 00 - Teste de Integração: Deve retornar erro para tipo de dano inválido")
	void tipoDanoOuDefesaComDanoInvalido() {
		//Arrange;
		String tipoEquipamento = "espda";
		String tipoDanoInicial = "Z"; //inválido;
		int valor = 10;
		
		//Act;
		String resultado = aprimoramentoService.tipoDanoOuDefesa(tipoEquipamento, tipoDanoInicial, valor);
		
		//Assert;
		assertThat(resultado).isEqualTo("Tipo de dano inválido. Use: C (Corte), P (Perfuração), B (Balístico) ou I (Impacto).");
	}
}
*/
/*

*/